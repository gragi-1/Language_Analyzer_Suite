// Programa 7: Tipos incompatibles
let int numero = 10;
let string texto = '5';
let boolean flag = true;

if (numero) {  // ERROR SEMÁNTICO: condición no booleana (falta comparación)
    texto += numero;  // ERROR SEMÁNTICO: string += int (tipos incompatibles)
    numero = texto;  // ERROR SEMÁNTICO: int = string
}

if (flag < numero) {  // ERROR SEMÁNTICO: boolean < int
    write 'Imposible';
}

write numero;
