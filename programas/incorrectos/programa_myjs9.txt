// Programa 9: Mezcla de errores
let int a = 10;
let int b = 20;
let string resultado;

if (a < b && ) {  // ERROR SINTÁCTICO: && sin segundo operando
    resultado = 'a menor que b';
    a += b;
}

// ERROR SEMÁNTICO: uso incorrecto de +=
resultado += 5;  // string += int

// ERROR: Punto y coma faltante
write resultado
write a
