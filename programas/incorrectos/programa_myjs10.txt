// Programa 10: Estructura confusa
let int contador = 0;
let string resultado = '';

if (contador < 10)
    contador += 1;
    resultado = 'Incrementado';  // ERROR: sentencias sin llaves (depende de gramática)
else  // ERROR SINTÁCTICO: else sin if correspondiente
    resultado = 'Fin';

// ERROR: Operador mal usado
let int x = 5;
x =+ 3;  // ERROR LÉXICO/SINTÁCTICO: =+ en lugar de +=

// Comentario con error léxico implícito
// Esto está 'bien  // ERROR: comilla simple dentro de comentario podría confundir

write resultado;
