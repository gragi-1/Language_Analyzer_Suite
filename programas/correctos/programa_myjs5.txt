// Programa 5: Validaciones encadenadas
let int edad = 18;
let boolean tieneCarnet = true;
let boolean padresAutorizan = false;
let string puedeConducir;

if (edad < 18 && padresAutorizan) {
    puedeConducir = 'Con supervisión';
    edad += 1;
} else {
    if (edad < 18) {
        puedeConducir = 'No puede';
    } else {
        if (tieneCarnet) {
            puedeConducir = 'Puede conducir';
        } else {
            puedeConducir = 'Neita carnet';
        }
    }
}

write puedeConducir;
