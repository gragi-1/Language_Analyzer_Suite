// Programa 2: Uso correcto de &&
let int x = 5;
let int y = 8;
let boolean activo = true;

if (x < y && activo) {
    x += y;
    write 'Suma realizada: ';
    write x;
} else {
    write 'No se cumple';
}
