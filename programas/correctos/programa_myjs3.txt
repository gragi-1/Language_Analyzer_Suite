// Programa 3: Estructuras anidadas
let int contador = 0;
let int limite = 5;
let string estado = 'inicio';

if (contador < limite) {
    contador += 1;
    if (contador < 3) {
        estado = 'temprano';
    } else {
        estado = 'avanzado';
    }
}

write estado;
write contador;
